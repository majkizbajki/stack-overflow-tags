import { Colors } from "@/lib/theme/colors";
import { Typography } from "@mui/material";

export default function Home() {
  return (
    <main>
      <Typography variant="body1">Stack Overflow Tags</Typography>
    </main>
  );
}
